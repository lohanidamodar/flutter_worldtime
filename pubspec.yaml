name: flutter_worldtime
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^1.0.4
  hooks_riverpod: ^1.0.4
  http: ^0.13.5
  intl: ^0.17.0
  state_notifier: ^0.7.2+1
  dropdown_search: ^5.0.2
  equatable: ^2.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

